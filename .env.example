API_BASE_URL=https://api.example.com
# Unset or empty uses the site root. Set a subdirectory when hosting there.
APP_BASE_PATH=/
# Optional output-directory override; empty keeps the framework default.
BUILD_OUTPUT=
